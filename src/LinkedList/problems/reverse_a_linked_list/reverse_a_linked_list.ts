import { LinkedList } from '../../LinkedList';

export function reverseList(list: LinkedList): LinkedList {
  // I implemented the reverse method myself so don't judge me.😁🏃🏿‍♂️
  return list.reverse();
}
